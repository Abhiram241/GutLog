/**
 * app/index.tsx
 *
 * Entry point for the Expo Router app.
 * Renders the AppNavigator which contains all screens and logic.
 */

import { AppNavigator } from "@/src/navigation/AppNavigator";

export default AppNavigator;
